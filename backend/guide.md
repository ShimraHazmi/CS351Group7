backend/                # Outer folder (your project root for Django)
├── manage.py           # Command-line utility for running the project
└── backend/            # Inner folder (the actual Python package for your project)
    ├── __init__.py
    ├── settings.py     # Project settings
    ├── urls.py         # URL routing
    ├── asgi.py         # ASGI entry point
    └── wsgi.py         # WSGI entry point